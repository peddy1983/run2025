# 版本控制說明

## 📌 當前版本

**v1.0.0** - 2025-11-06

---

## 🏷️ 版本管理

本專案使用 **Git Tags** 進行版本管理。

### 查看所有版本
```bash
git tag
```

### 查看當前版本
```bash
git describe --tags
```

### 回到特定版本
```bash
# 查看版本內容（不改變工作目錄）
git show v1.0.0

# 切換到特定版本（建立新分支）
git checkout -b version-1.0.0 v1.0.0

# 回到最新版本
git checkout master
```

---

## 📦 版本備份

### v1.0.0 完整功能清單

#### ✅ 用戶系統
- Email/Password 註冊登入
- 用戶資料管理
- 權限控制

#### ✅ 活動管理
- 發起活動
- 編輯活動
- 刪除活動
- 加入活動
- 取消參加

#### ✅ 頁面功能
- 首頁（活動列表 + 日期選擇器）
- 我要揪團
- 我的活動
- 登入/註冊頁面
- 編輯活動頁面

#### ✅ UI 元件
- Navbar（頂部導航 + 底部導航）
- DatePicker（日期選擇器）
- ActivityCard（活動卡片）

#### ✅ 技術架構
- React 18 + Vite
- Firebase Auth + Firestore
- Tailwind CSS
- React Router
- date-fns

---

## 🔄 版本更新流程

### 1. 開始新版本開發
```bash
# 從 v1.0.0 建立新的開發分支（可選）
git checkout -b dev-v1.1.0
```

### 2. 進行修改並提交
```bash
git add .
git commit -m "feat: add new feature"
```

### 3. 完成開發後標記新版本
```bash
# 合併回主分支（如果使用分支開發）
git checkout master
git merge dev-v1.1.0

# 標記新版本
git tag -a v1.1.0 -m "Version 1.1.0 - New features"

# 更新 package.json 版本號
# 更新 CHANGELOG.md
```

### 4. 部署新版本
```bash
npm run build
# 上傳 dist 到 Netlify
```

---

## 🔙 緊急回滾

如果新版本出現嚴重問題，可以快速回滾到 v1.0.0：

```bash
# 1. 回到 v1.0.0
git checkout v1.0.0

# 2. 重新打包
npm install
npm run build

# 3. 部署穩定版本的 dist 資料夾到 Netlify
```

---

## 📊 版本號規則

```
主版本號.次版本號.修訂號
   |       |       |
   |       |       └─ 修復 Bug、小改善
   |       |
   |       └───────── 新增功能（向下相容）
   |
   └───────────────── 重大變更（不相容更新）
```

### 範例
- `1.0.0` → `1.0.1`: 修復小 Bug
- `1.0.0` → `1.1.0`: 新增功能
- `1.0.0` → `2.0.0`: 重大架構變更

---

## 🗂️ 重要檔案備份

### 核心設定檔
- `package.json` - 依賴套件版本
- `firebase/config.js` - Firebase 設定
- `.env` - 環境變數（不在 Git 中）
- `netlify.toml` - 部署設定
- `FIRESTORE_RULES.md` - 資料庫規則

### 建議定期備份
1. `.env` 檔案（本地保存）
2. Firebase 專案設定
3. Netlify 環境變數設定
4. `dist` 資料夾（當前部署版本）

---

## 💡 最佳實踐

1. **每次重大更新前建立 Tag**
   ```bash
   git tag -a v1.x.x -m "描述"
   ```

2. **更新 CHANGELOG.md**
   - 記錄所有變更
   - 分類：新增/修復/改善/移除

3. **更新 package.json 版本號**
   ```json
   "version": "1.x.x"
   ```

4. **測試後再部署**
   ```bash
   npm run dev  # 本地測試
   npm run build  # 確認打包成功
   ```

5. **保留上一版本的 dist 資料夾**
   - 複製 `dist` 為 `dist-v1.0.0`（本地備份）

---

**建立日期**: 2025-11-06  
**維護者**: Running Group Admin

